<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>bio by darkzzy</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      height: 100vh;
      background-color: black;
      font-family: 'Poppins', sans-serif;
      overflow: hidden;
      display: flex;
      justify-content: center;
      align-items: center;
      color: white;
      text-align: center;
    }
    #loader {
      position: absolute;
      width: 100%;
      height: 100%;
      background: black;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 2rem;
      animation: fadeOut 1s ease forwards;
      animation-delay: 2s;
      z-index: 1000;
    }
    @keyframes fadeOut {
      to {
        opacity: 0;
        visibility: hidden;
      }
    }
    canvas {
      position: absolute;
      top: 0;
      left: 0;
      z-index: 0;
    }
    .content {
      position: relative;
      z-index: 1;
      animation: fadeIn 2s ease;
      opacity: 0;
      animation-fill-mode: forwards;
      animation-delay: 2s;
    }
    .content h1 {
      font-size: 3rem;
      animation: colorChange 5s infinite alternate;
    }
    .links {
      margin-top: 20px;
    }
    .links a {
      display: inline-block;
      margin: 10px;
      color: white;
      text-decoration: none;
      border: 2px solid white;
      padding: 10px 20px;
      border-radius: 10px;
      transition: 0.3s;
    }
    .links a:hover {
      background-color: white;
      color: black;
    }
    .skills {
      margin-top: 30px;
      font-size: 1.2rem;
    }
    @keyframes fadeIn {
      from {opacity: 0;}
      to {opacity: 1;}
    }
    @keyframes colorChange {
      0% {color: white;}
      50% {color: #00ffff;}
      100% {color: #ff00ff;}
    }
  </style>
</head>
<body>

<div id="loader">Loading...</div>

<canvas id="canvas"></canvas>

<div class="content">
  <h1>bio by darkzzy</h1>

  <div class="links">
    <a href="https://t.me/zero421s" target="_blank">Telegram</a>
  </div>

  <div class="skills">
    <p>Frontend & Backend Developer</p>
  </div>
</div>

<audio autoplay loop>
  <source src="https://files.catbox.moe/3x9kde.mp3" type="audio/mpeg">
</audio>

<script>
  const canvas = document.getElementById('canvas');
  const ctx = canvas.getContext('2d');
  canvas.width = window.innerWidth;
  canvas.height = window.innerHeight;

  let particlesArray = [];

  window.addEventListener('resize', function(){
    canvas.width = window.innerWidth;
    canvas.height = window.innerHeight;
  });

  const mouse = {
    x: null,
    y: null
  }

  window.addEventListener('mousemove', function(event){
    mouse.x = event.x;
    mouse.y = event.y;
    for (let i = 0; i < 5; i++) {
      particlesArray.push(new Particle());
    }
  });

  class Particle {
    constructor() {
      this.x = mouse.x;
      this.y = mouse.y;
      this.size = Math.random() * 5 + 1;
      this.speedX = Math.random() * 3 - 1.5;
      this.speedY = Math.random() * 3 - 1.5;
      this.color = 'white';
    }
    update(){
      this.x += this.speedX;
      this.y += this.speedY;
      this.size *= 0.95;
    }
    draw(){
      ctx.fillStyle = this.color;
      ctx.beginPath();
      ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2);
      ctx.fill();
    }
  }

  function handleParticles(){
    for (let i = 0; i < particlesArray.length; i++){
      particlesArray[i].update();
      particlesArray[i].draw();
      if (particlesArray[i].size < 0.3){
        particlesArray.splice(i, 1);
        i--;
      }
    }
  }

  function animate(){
    ctx.clearRect(0, 0, canvas.width, canvas.height);
    handleParticles();
    requestAnimationFrame(animate);
  }

  animate();
</script>

</body>
</html>
